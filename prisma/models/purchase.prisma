model Purchase {
  id_purchase   Int      @id @default(autoincrement())
  purchase_date DateTime
  supplier_name String
  description   String?
  quantity      Int
  price         Float
  created_at    DateTime @default(now())

  id_construct Int
  construct    Construct @relation(fields: [id_construct], references: [id_construct])
}